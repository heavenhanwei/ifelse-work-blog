{
  "name": "ifelse-work-blog",
  "version": "1.0.0",
  "description": "博客页面 - ifelse.work",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=ifelse-work-blog",
    "deploy:ci": "npm run build && wrangler pages deploy dist --project-name=ifelse-work-blog --commit-dirty=true"
  },
  "dependencies": {
    "marked": "^18.0.4",
    "vue": "^3.5.0",
    "vue-router": "^4.6.4"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.0",
    "vite": "^6.2.0",
    "wrangler": "^4.94.0"
  }
}
